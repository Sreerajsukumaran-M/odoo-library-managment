<odoo>
    <record id="action_library_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module' : 'library_management'}</field>
    </record>
    <record id="settings_list_view" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.library_management.library_settings</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="5"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="library settings"
                     string="library " name="library_management">
                    <block title="library Management Settings"
                           name="employee_management">
                        <setting string="max borrowed days"
                                 help="Select max borrowed days">
                            <field name="max_borrowed_days"/>
                        </setting>
                        <setting string="max remainder days"
                                 help="Select remainder days">
                            <field name="reminder_days"/>
                        </setting>
                        <setting string="penalty charged (In hours)"
                                 help="penalty amount charged in hours ">
                            <field name="penality_amount"/>
                        </setting>
                        <setting string="maximum books"
                                 help="maximum book can borrow ">
                            <field name="maximum_book"/>
                        </setting>
                        <setting string="maximum late return"
                                 help="maximum book late return">
                            <field name="maximum_late_return"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>